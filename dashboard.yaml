---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard
  namespace: traefik-system
spec:
  routes:
  - match: Host(`dashboard.k8s.opvolger.eu`)
    kind: Rule
    services:
    - name: api@internal
      kind: TraefikService